import { TruncateAtPointPipe } from './truncate-at-point.pipe';

describe('TruncateAtPointPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateAtPointPipe();
    expect(pipe).toBeTruthy();
  });
});
